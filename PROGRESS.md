# 작업 기록 (PROGRESS)

> 이 파일은 세션 간 작업 연속성을 위한 기록입니다.
> - 새 세션 시작 시 이 파일을 먼저 확인합니다.
> - 기능 하나가 완료될 때마다 아래 체크리스트에 체크(완료 날짜 포함)하고, 맨 아래 "작업 로그"에 새 항목을 누적 기록합니다.
> - 기존 기록은 지우지 않고 계속 추가합니다.

---

## 진행 체크리스트

### 1단계. 프로젝트 기본 설정
- [x] 파일 구조 생성 (2026-07-27)
- [x] Git 설정 (2026-07-27)

### 2단계. 메인화면 & 카테고리 구조
- [x] 메인화면 디자인 초안 (공통/개인/사업자/추가 사업체 버튼, 이름 변경 기능) (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 배포하기 (초안 확인용) (2026-07-28, https://kmemo-nine.vercel.app/ , 1차·2차 확인 모두 완료)
- [x] 메모 저장/불러오기 (2026-07-27, 1차·2차 확인 모두 완료)
- [x] CRUD 기능 (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 카테고리 다중 선택 기능 (2026-07-27, 1차·2차 확인 모두 완료)

### 3단계. 캘린더 화면
- [x] 메인화면 하단 미니 달력(월 그리드, 이전/다음 달 이동, 메모 작성일에 사용자 지정 색 점 표시, 오늘 날짜 빨간 테두리) (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 공통/개인/사업자/추가 사업체 클릭 시 카테고리 전용 대시보드로 이동 (달력 + 그날 메모 + 이번 달 할 일 + 사업자 필수 항목) (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 그날 메모 영역: 대시보드 달력에서 날짜 클릭 → "오늘의 메모"/"[N]월[N]일의 메모" 카드에 해당 날짜 메모 표시 (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 월간 할 일 메모 영역: "이번 달 할 일" 카드, 체크박스로 완료 처리 (2026-07-27, 1차·2차 확인 모두 완료)
- [x] 사업자 필수 항목 영역: 모든 카테고리에 동일 표시(사용자 선택), 완료/관찰중 처리 (2026-07-27, 1차·2차 확인 모두 완료)

### 4단계. 체크리스트 상태 표현
- [x] 완료 체크박스 + 불투명 회색 처리 (이번 달 할 일 / 사업자 필수 항목 카드 항목에 적용) (2026-07-27, 1차·2차 확인 모두 완료)
- [x] "지켜봐야 함(관찰중)" 표시 (두꺼운 테두리 + 우측 상단 작은 글씨 "관찰중" 배지) (2026-07-27, 1차·2차 확인 모두 완료)

### 5단계. 알림/주기 기능
- [ ] 날짜 직접 지정(복수)
- [ ] N일 주기 반복 알림

### 6단계. 검색 & PWA
- [ ] 메모 내용 기반 검색
- [ ] 오프라인 지원 (Service Worker, Manifest)

### 7단계. UI 확장
- [ ] (체크리스트 미정 — CLAUDE.md에 항목 추가 시 함께 갱신)

### 8단계. 로그인 & 기기 간 동기화 (사용자 요청으로 신규 추가, 2026-07-28) — 1차·2차 확인 모두 완료
- [x] 로그인 화면(아이디+비밀번호, 최초 PIN 방식에서 사용자 요청으로 변경) & 세션 유지 & 로그아웃 (2026-07-28, 1차·2차 확인 모두 완료)
- [x] Supabase 스키마(`supabase/schema.sql`: users(username/password_hash)/data 테이블 + auth_login/get_memos/sync_memos RPC) 작성 (2026-07-28)
- [x] 실제 Supabase 프로젝트 연결 (`js/supabase-config.js`에 URL/anon key 입력) 완료, 1차·2차 확인 모두 완료 (2026-07-28)
- [x] 메모 저장 시 자동 백그라운드 동기화 + 병합 로직, 다른 기기 로그인 시 자동 반영 버그 수정 포함 (2026-07-28, 1차·2차 확인 모두 완료 — 폰↔PC 확인)
- [x] 메인화면 우측 하단 "⟳ 업데이트" 수동 동기화 버튼 (2026-07-28, 1차·2차 확인 모두 완료)

---

## 현재 상태 요약

- 메인화면 UI 완료: 공통(대형 버튼) / 개인 / 사업자 버튼, "+ 사업체 추가" 버튼, 앱 이름·버튼 이름 변경(✏) 기능
- 메모 CRUD 완료: 카테고리 대시보드에서 "+ 메모 추가"(일반) / "+ 할 일 추가"(이번 달 할 일) / "+ 필수 항목 추가"(사업자 필수)로 작성, 카테고리 다중 선택 + 색상 지정, 수정/삭제 가능
- 카테고리 대시보드 화면 완료: 공통/개인/사업자/추가 사업체 버튼 클릭 → 카테고리 전용 미니 달력(날짜 클릭 가능) + "오늘의 메모"(선택 날짜의 일반 메모) + "이번 달 할 일"(체크박스, 완료 시 회색 처리) + "사업자 필수 항목"(모든 카테고리 공통 표시, "관찰중" 배지) + 각 카드 "전체보기"
- 메인화면(버튼 누르기 전) 맨 아래에도 전체 카테고리 통합 미니 달력 표시, 오늘 날짜는 얇은 빨간 테두리로 표시
- 데이터는 `localStorage`(`kmemo_data` 키)에 저장되어 새로고침해도 유지됨 (이전 버전 데이터 호환 처리 포함)
- **1단계~4단계 관련 항목 모두 1차·2차 확인 완료** (2026-07-27)
- **GitHub 연동 완료**: https://github.com/hyuuni9704/Kmemo (main 브랜치, 첫 커밋 "first commit" 푸시 완료, working tree clean)
- 사용자 요청으로 이번 세션은 여기서 일시 중단. 다음 세션에서 "계속진행"이라고 말하면 이어서 진행할 것.
- 다음 작업 후보 (아직 미결정, 다음 세션에서 사용자와 확정): **2단계 - 배포하기(Vercel)** / **5단계 - 알림·주기 기능** / **6단계 - 검색 기능**

---

## 작업 로그

### 2026-07-27
- `PROGRESS.md` 최초 생성. 아직 구현된 기능 없음. 1단계(프로젝트 기본 설정)부터 진행 예정.
- 1단계 완료: `index.html`, `css/style.css`, `js/main.js` 기본 파일 구조 생성, `git init`으로 저장소 초기화. 아직 UI/기능 없음(2단계에서 메인화면 구성 예정).
- 2단계 "메인화면 디자인 초안" 완료: 공통/개인/사업자 버튼 + 사업체 추가 + 이름 변경(앱 이름 포함) 기능을 localStorage 연동까지 구현. Chrome 헤드리스로 1차 자동화 확인(렌더링/콘솔 에러 없음) 완료.
- 2차 실기기 확인 완료: 로컬 네트워크(LAN IP)로 폰 브라우저 접속하여 앱 이름 변경, 버튼 이름 변경, 사업체 추가, 선택 강조, 새로고침 후 유지 5개 항목 모두 정상 확인. 이 기능은 완료 처리.
- 2단계 "메모 저장/불러오기 + CRUD + 카테고리 다중 선택" 완료: 메인화면 버튼 클릭 시 카테고리별 메모 목록으로 이동하도록 변경, 메모 추가/수정/삭제, 카테고리 다중 선택 태그, localStorage 저장을 구현. 헤드리스 브라우저로 추가→수정→필터링→삭제 전체 흐름 자동 테스트하여 1차 확인 완료. 2차 실기기 확인 대기 중.
- 2차 실기기 확인 결과 문제 발견: 메모 추가 저장 후 입력칸에 글씨가 그대로 남는 문제. 원인은 저장 후 목록 화면으로 이동하면서 입력칸을 비우지 않던 구조였음. 저장 시 같은 화면에서 입력칸만 비우고 바로 다음 메모를 이어 쓸 수 있게 수정. 테스트 중 "뒤로가기 시 방금 추가한 메모가 목록에 안 보이는" 연쇄 버그도 함께 발견해 수정(뒤로가기 시 목록 다시 렌더링하도록 처리).
- 메인화면 하단 미니 달력 기능 추가: 메모에 `color` 필드 추가(메모 작성 화면에서 색상 선택), 메인화면 최하단에 월 그리드 미니 달력을 붙여 이전/다음 달 이동 및 메모 작성일(createdAt)에 지정 색 점 표시. CLAUDE.md 메모 데이터 구조에 `color` 필드 문서화.
- 기능 검증 중 렌더링 버그 발견: `.view` 클래스의 `display:flex`가 `[hidden]` 속성보다 우선 적용되어 목록/작성 화면이 메인화면 아래에 겹쳐 보임. `.view[hidden] { display: none; }` 규칙 추가로 수정. 이후 실제 index.html 구조와 동일한 통합 테스트로 연속 메모 작성→목록 갱신→수정→필터링→삭제→달력 점 반영까지 전체 흐름 1차 확인 완료. 2차 실기기 재확인 대기 중.
- 카테고리 대시보드 화면(3단계+4단계 상당) 구현: 기존 단순 메모 목록 화면을 "카테고리 전용 달력 + 오늘의 메모 + 이번 달 할 일 + 사업자 필수 항목" 대시보드로 전면 교체. 메모에 `scope`(구분: general/calendar-month/business-required)와 `date` 필드를 실제로 활용하도록 작성 화면에 날짜 입력·구분 라디오·완료/관찰중 토글 추가. 이번 달 할 일·사업자 필수 항목은 체크박스 완료 시 회색 처리, 관찰중 시 두꺼운 테두리+배지 표시. 사업자 필수 항목은 사용자 선택에 따라 모든 카테고리에서 동일하게 노출. 각 카드에 "전체보기" 전용 목록 화면 추가. 헤드리스 브라우저로 대시보드 진입/날짜별 필터링/체크박스 완료/관찰중 배지/카테고리 간 전역 표시/전체보기/수정·삭제/메인 달력 연동까지 전체 시나리오 1차 자동화 확인 완료.
- 미니 달력(메인화면 + 대시보드) 오늘 날짜에 얇은 빨간 테두리 표시 추가.
- 2차 실기기 확인 완료: 카테고리 대시보드 전체 기능(달력 이동, 날짜 클릭 필터링, 메모/할일/필수항목 추가, 완료 회색 처리, 관찰중 표시, 전체보기, 오늘 날짜 빨간 테두리 등) 폰에서 모두 정상 동작 확인. 1단계~4단계 관련 체크리스트 전부 완료 처리.
- GitHub 연동: `README.md` 생성 후 프로젝트 전체 파일을 첫 커밋(`first commit`)으로 추가, `main` 브랜치로 이름 변경, `https://github.com/hyuuni9704/Kmemo.git`을 origin으로 등록하고 푸시 완료. (git 사용자 정보(name/email)는 사용자가 직접 설정)
- 사용자 요청으로 세션 일시 중단. 다음 작업(배포/알림/검색 등)은 아직 미확정 — 다음 세션 시작 시 "계속진행" 요청과 함께 어떤 기능부터 할지 사용자에게 다시 확인할 것.

### 2026-07-28
- 2단계 "배포하기(초안 확인용)" 진행: 사용자가 Vercel 대시보드에서 GitHub 저장소(`hyuuni9704/Kmemo`)를 Import하여 배포 완료. 배포 URL: https://kmemo-nine.vercel.app/ . WebFetch로 메인화면(오늘의 메모/이번 달 할 일/사업자 필수 항목 등) 정상 로딩 1차 자동화 확인 완료. CLAUDE.md "배포 URL" 항목 및 체크리스트 갱신. 이후 사용자가 폰 브라우저로 접속해 정상 동작 2차 확인 완료 — "배포하기" 기능 최종 완료 처리.

### 2026-07-28 (8단계 신규 추가: 로그인 & 기기 간 동기화)
- 사용자 요청으로 체크리스트에 없던 "첫화면 로그인" 기능을 새로 추가하기로 함. 이 앱이 서버 없는 완전 오프라인 구조라 "로그인"의 의미(계정 인증 vs 단순 잠금)를 먼저 사용자와 확정: PIN(4자리) 잠금 + 동일 PIN으로 여러 기기 인증 시 메모 데이터 동기화까지 필요하다고 확인. 이는 서버/DB 신규 도입이 필요한 큰 변경이라 오프라인 우선 원칙과의 충돌, PIN 보안 취약성(4자리는 무차별 대입에 약함, 사용자가 인지하고 감수하기로 함), 백엔드 선택(Supabase 선택)을 순서대로 확인 후 진행.
- `supabase/schema.sql` 작성: `kmemo_users`(pin_hash)/`kmemo_data`(user_id, data jsonb) 테이블 + `auth_login`/`get_memos`/`sync_memos` RPC 함수. 원본 테이블은 RLS + 권한 회수로 완전 차단하고, PIN을 매 호출마다 해시 비교로 재검증하는 SECURITY DEFINER 함수로만 접근하도록 구성해 4자리 PIN의 약점을 최소화. 사용자가 Supabase 프로젝트를 만들어 SQL Editor에서 직접 실행해야 함 (아직 미실행, URL/anon key도 미제공 → `js/supabase-config.js`는 플레이스홀더 상태).
- `js/sync.js`(신규) 추가: 세션(`kmemo_session`) 저장/조회, `loginWithPin`, 서버↔로컬 메모 병합(`updatedAt` 기준 최신 우선), 자동 백그라운드 동기화(`syncPushInBackground`, `saveData()` 호출 시마다 실행), 수동 동기화(`manualSync`) 구현.
- `index.html`에 로그인 화면(`view-login`, PIN 4자리 입력) 추가 및 스크립트 로드 순서 정리(supabase-js CDN → supabase-config → sync → main). 헤더에 로그아웃 버튼 추가.
- 사용자 추가 요청: 메인화면 우측 하단에 작게 "업데이트" 버튼을 만들어 온라인일 때 누르면 같은 계정으로 로그인된 다른 기기의 메모가 반영되게 해달라는 요청 → 같은 8단계 기능으로 판단해 함께 진행. `updateSyncBtn` 버튼과 `handleManualSyncClick` 핸들러 추가(동기화 중/완료/실패 상태 텍스트 표시).
- `js/main.js` 수정: `init()`을 세션 유무에 따라 로그인 화면 또는 메인 앱으로 분기하도록 재구성(`bootApp` 역할), 기존 초기화 로직은 `initMainApp()`으로 이동, `saveData()`가 저장 시마다 `syncPushInBackground()` 호출하도록 변경.
- 1차 자동화 확인: node/npm/python이 없는 환경이라 Chrome 헤드리스(`chrome.exe --headless=new --dump-dom`)로 직접 테스트. 처음엔 `file://` 경로의 드라이브 문자 형식 오류, 그다음 인라인 `<script defer>`가 스펙상 무시되는 문제(스크립트가 main.js보다 먼저 실행되어 `bindLoginView is not defined` 오류)를 차례로 해결한 뒤, PIN 형식 오류 검증("4자리 숫자를 입력해주세요" 정상 표시)과 세션 우회 시 메인화면 진입(로그인 화면 숨김, 로그아웃/업데이트 버튼 노출, 버튼이 실제 뷰포트 안에 위치)까지 전부 정상 확인.
- 2차 실기기 확인은 사용자가 Supabase 프로젝트를 생성하고 `supabase/schema.sql` 실행 + `js/supabase-config.js`에 URL/anon key 입력을 완료한 뒤 진행 예정 (대기 중).
- 사용자가 Supabase 프로젝트 URL(JWT anon key의 `ref` 클레임으로 역산: `ubxlxnhoielyvnfjiovp`)과 anon key를 제공 → `js/supabase-config.js`에 반영. curl로 RPC 직접 호출해 `auth_login`(신규 등록/동일 PIN 재로그인/틀린 PIN 거부)·`get_memos`·`sync_memos` 전부 정상 동작 확인. 처음엔 `search_path = public`만 지정해 Supabase가 `extensions` 스키마에 설치하는 `pgcrypto`의 `digest()`를 못 찾는 오류(`function digest(text, unknown) does not exist`) 발생 → `search_path = public, extensions`로 수정 후 재실행하여 해결. 실제 앱 코드(`js/sync.js`/`js/main.js`)로 헤드리스 브라우저에서 진짜 로그인 + 서버에 올려둔 테스트 메모가 병합되어 내려오는 것까지 end-to-end로 확인 완료. 이후 CLAUDE.md/PROGRESS.md 최종 반영 후 커밋(`dd4784f`) 및 GitHub 푸시, Vercel 자동 재배포 확인(https://kmemo-nine.vercel.app/ 에서 로그인 화면 정상 노출 WebFetch로 확인).
- 사용자가 "첫 로그인 시 아이디와 비번을 치고 두 번째부터는 자동 로그인"을 요청 → 이미 자동 로그인은 구현돼 있었으나, 인증 방식 자체를 PIN(4자리)에서 **아이디+비밀번호**로 바꿔달라는 의미였음을 확인 후 진행. `supabase/schema.sql`을 아이디/비밀번호 방식으로 전면 재작성(기존 PIN 기반 테이블/함수 `drop` 후 재생성 — 테스트 계정만 있던 단계라 데이터 보존 불필요): `kmemo_users(username, password_hash)`, `auth_login(p_username, p_password)`(신규 아이디는 자동 등록, 기존 아이디는 비밀번호 해시 비교 후 불일치 시 `WRONG_PASSWORD` 예외), `get_memos`/`sync_memos`도 동일하게 아이디+비밀번호 재검증으로 변경. `js/sync.js`의 `loginWithPin` → `loginWithCredentials(username, password)`로 교체, 세션 구조를 `{userId, username, password}`로 변경. `index.html` 로그인 화면을 PIN 입력 1칸에서 아이디/비밀번호 2칸 입력으로 교체, `js/main.js`의 `handleLoginSubmit`에 아이디 필수·비밀번호 4자 이상 검증과 틀린 비밀번호 전용 에러 메시지 추가. 사용자가 새 SQL 스크립트를 재실행한 뒤 curl과 헤드리스 브라우저로 재검증(신규 아이디 등록/동일 자격증명 재로그인/틀린 비밀번호 거부/실제 로그인 성공까지) 전부 통과 확인.
- 사용자가 실기기 2차 확인 중 "다른 기기에서 같은 아이디/비번으로 로그인은 되는데 메모가 안 보임" 문제를 보고. 원인을 두 가지로 특정: (1) 예전 PIN 방식 배포 때 이미 자동 로그인 세션이 저장된 기기는 `{userId, pin}` 형식 그대로 남아있어, 아이디/비밀번호로 바뀐 이후에도 그 기기가 계속 옛 세션으로 자동 진입하면서 `username`/`password`가 없는 상태로 동기화를 시도 → 서버가 조용히 거부(console.warn만 남고 화면엔 에러 없음)해서 그 기기의 메모가 애초에 서버로 한 번도 올라간 적이 없었음. (2) 새 기기에서 로그인 성공 직후 `initMainApp()`으로 화면을 먼저 그리고 `syncOnLoad()`는 백그라운드로 돌리는 구조라, 병합이 끝나기 전에 화면이 그려지는 타이밍 문제도 존재.
  - 수정 1: `js/sync.js`의 `loadSession()`이 `username`/`password` 필드가 없는(형식이 안 맞는) 세션을 무효로 간주하고 자동으로 지운 뒤 `null`을 반환하도록 변경 → 예전 세션이 남은 기기는 다음 접속 시 강제로 로그인 화면이 다시 뜸.
  - 수정 2: `js/main.js`에 `startMainAppAfterLogin()`을 새로 분리해 로그인 직후에는 `syncOnLoad()`가 끝날 때까지 기다린 뒤 화면을 그리도록 변경(로그인 버튼에 "동기화 중..." 표시). 기존 세션으로 재진입하는 경우(`startMainApp()`)는 오프라인 우선 동작을 유지하기 위해 그대로 로컬 데이터로 즉시 렌더링 후 백그라운드 동기화.
  - 완전히 분리된 두 Chrome 프로필(서로 다른 `--user-data-dir`, 로컬 스토리지 공유 전혀 없음)로 "계정 A가 메모 생성 → 계정 B(신규 기기, 세션 없음)가 로그인만 하면 업데이트 버튼 없이 메모가 즉시 보임" 시나리오를 실제 Supabase 서버로 재현해 1차 자동화 확인 완료.
- 2차 실기기 확인: 사용자가 폰에서 예전 세션이 자동으로 무효화되어 재로그인 후 정상 동기화되는 것을 확인. PC에서는 처음에 "폰에서 등록한 메모가 안 보인다"고 했으나, 원인은 PC 브라우저가 수정 전 버전의 `index.html`/`js` 파일을 캐시하고 있던 것 → 강력 새로고침(Ctrl+Shift+R) 후 정상 동기화 확인. 사용자가 최종적으로 폰↔PC 간 동기화까지 확인 완료를 알려와 8단계(로그인 & 기기 간 동기화) 전체 체크리스트를 1차·2차 확인 완료로 처리.
