# 메모앱 개발가이드

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 오프라인 메모장 애플리케이션 (개인/사업자 겸용)
- **프로젝트명**: (미정 — 사용자가 메인화면에서 직접 이름 지정 가능)
- **배포 URL**: https://kmemo-nine.vercel.app/

### 목적
- 개인 일정/할 일과 사업자(자영업 등) 업무를 한 앱에서 관리하되, 완전히 섞이지 않도록 카테고리로 구분해서 기록·확인할 수 있는 메모/캘린더 앱

### 타깃 사용자
- 개인 용무와 사업자(자영업) 용무를 함께 관리해야 하는 1인 사업자 / 다중 사업체 운영자

### 톤 & 비주얼 컨셉
- 

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- 

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 여기서 "단계"는 1~9단계 같은 큰 번호 순서가 아니라 **하나의 완결된 기능 단위**(예: 헤더, 슬라이더, 더보기 메뉴의 "내보내기" 기능 등)를 뜻함.
  - 기능 하나가 UI(예: 3단계)와 실제 동작 로직(예: 5단계)처럼 서로 다른 번호의 체크리스트에 걸쳐 있을 수 있음. 이런 경우 UI만 먼저 만들고 로직은 나중에 따로 만들지 않고, **기능이 실제로 동작하도록 관련 항목을 한 번에 묶어서** 진행하는 것을 기본으로 함 (재작업 비용을 줄이기 위함).
  - 다만 서로 다른 기능을 한 세션에 여러 개 묶어 진행하는 것은 여전히 금지 (절대 금지 사항 참고).
- 승인은 **세션 전체에 한 번이 아니라, 기능 단위마다 매번** 받을 것.
- 한 기능이 끝나면 자동으로 다음 기능으로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."
  - 하나의 기능이 여러 번호의 체크리스트 항목을 함께 완료시키는 경우, 계획 설명 시 어느 항목들이 함께 체크되는지 명시할 것.

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행하되, 번호 순서는 권장 순서일 뿐 절대 규칙은 아님 — 진행 중인 기능이 다른 번호의 항목과 연결되어 있다면 그 항목까지 함께 진행 가능 (위 "1. 승인 원칙" 참고)
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

### 3. 검증 규칙 (기능 구현 후 확인)
- 자동화 테스트(로컬 서버/헤드리스 브라우저 등, `localhost`·`file://` 기준)를 통과했다고 해서 실제 환경에서도 정상 동작한다고 단정하지 말 것.
- 기능 구현 완료 후 확인은 2단계로 나눠 진행할 것.
  1. Claude가 자동화 테스트로 1차 확인
  2. 사용자가 실제 기기/네트워크 환경(폰 브라우저, 필요 시 PC를 LAN IP로 접속 등)에서 2차 확인
- 2차 확인이 끝나기 전까지는 다음 기능 단계로 넘어가지 말 것.

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지 (단, 하나의 기능이 여러 번호의 체크리스트 항목에 걸쳐 있는 경우는 예외 — 위 "1. 승인 원칙" 참고)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript
- PWA: Service Worker, Web App Manifest
- Storage: localStorage (오프라인 필수)
- Backup: JSON 파일 내보내기/가져오기
- 배포: Vercel
- 버전관리: GitHub

---

## 핵심 기능 요구사항 (사용자 정의)

### 1) 메인화면 구조
- 상단에 **공통** 큰 버튼 1개
- 그 아래 **개인 / 사업자 / 추가 사업체(사업체가 여러 개인 경우 계속 추가 가능)** 버튼
- 각 버튼을 누르면 해당 항목에 속한 메모만 필터링해서 확인 가능
- **공통**에서는 모든 카테고리의 메모를 통합해서 확인 가능
- 메모(또는 각 버튼)의 이름은 사용자가 직접 변경 가능

### 2) 일반 메모 기능
- 자유 형식의 일반 메모 작성 가능 (CRUD: 추가/수정/삭제/목록보기)

### 3) 캘린더 화면 구성
- 상단: 달력
- 달력 바로 아래
  - **왼쪽 위**: 선택한 날짜(그날)의 메모
  - **왼쪽 아래**: 그 달 전체에서 해내야 하는 일(월간 할 일) 메모
  - **오른쪽**: 사업자 전용 — 반드시 해야 하는 일 목록

### 4) 체크리스트(할 일) 상태 표현
- 완료 시 체크박스 체크 → 배경/텍스트를 **불투명한 회색**으로 처리해 미완료 항목과 시각적으로 구분
- "완료했지만 계속 지켜봐야 하는 항목"은
  - 카드/항목 **테두리를 두껍게** 표시
  - 메모 **우측 상단에 작은 글씨로 별도 표시** (예: "관찰중" 등)

### 5) 메모 카테고리 (다중 선택)
- 메모 작성 시 상단에 카테고리 체크박스 노출, **복수 선택 가능**
  1. 공통
  2. 개인
  3. 사업체
  4. 추가 사업체 (사업체가 여러 개인 사용자를 위한 확장 카테고리)

### 6) 알림/주기 기능
- 메모별로 "주시해야 하는 주기" 설정 가능
  - 옵션 A: 특정 날짜를 **여러 개 직접 지정**
  - 옵션 B: **N일 주기**로 반복 알림 받기 선택

### 7) 오프라인 지원
- 인터넷 연결 없이도 앱 사용 가능 (Service Worker + localStorage 기반 PWA)

### 8) 검색 기능
- 작성된 메모의 내용을 기준으로 검색해 과거 메모를 다시 찾을 수 있음

---

## 디자인 기획

### 컬러 시스템

#### Brand Colors



#### Light Mode


#### Dark Mode


#### CSS 변수 선언 규칙

### 타이포그래피


### 아이콘
- **라이브러리**: lucide-react

### UI 레이아웃 규칙


## 파일 구조


```
[프로젝트명]/
├── index.html          → 앱 엔트리 포인트
├── css/                  → 스타일시트
├── js/                   → 로직
└── [필요한 폴더 추가]
```

---

## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

---

## 메모 데이터 구조

```js
{
  id: 1,
  title: "메모 제목",
  content: "메모 내용",
  date: "2024-12-25",           // 특정 날짜 지정 메모/일정용
  categories: [],                // 다중 선택: "공통" | "개인" | "사업체" | "추가사업체명"
  color: "#4A90E2",              // 메인화면 미니 달력에 표시되는 점 색상 (사용자가 메모별로 직접 지정)
  isDone: false,                 // 체크박스 완료 여부
  isWatching: false,             // "완료했지만 계속 지켜봐야 함" 여부 (테두리 두껍게 + 우측 상단 표시)
  reminder: {
    type: null,                  // "dates" | "interval" | null
    dates: [],                   // type === "dates"일 때 사용, 여러 날짜 배열
    intervalDays: null           // type === "interval"일 때 사용, N일 주기
  },
  scope: "general",              // "general"(일반 메모) | "calendar-day"(그날 메모) | "calendar-month"(월간 할 일) | "business-required"(사업자 필수 항목)
  createdAt: "",
  updatedAt: ""
}
```

---

## 전체 진행 상황

---

## 1단계. 프로젝트 기본 설정

### 파일 구조 생성
- [x] index.html, css/style.css, js/main.js 기본 스켈레톤 생성 (2026-07-27)

### Git 설정
- [x] git init으로 저장소 초기화 (2026-07-27, 아직 커밋 없음)

---

## 2단계. 메인화면 & 카테고리 구조

### 메인화면 디자인 초안 잡기 (공통/개인/사업자/추가 사업체 버튼, 이름 변경 기능)
- [x] 공통(대형)/개인/사업자 버튼 + "+ 사업체 추가" + 앱 이름/버튼 이름 변경(localStorage 연동) (2026-07-27, 1차·2차 확인 모두 완료)


### 배포하기 (초안 확인용)
- [x] Vercel + GitHub(`hyuuni9704/Kmemo`) 연동 배포 (2026-07-28, 배포 URL: https://kmemo-nine.vercel.app/ , 1차·2차 확인 모두 완료)

### 메모 저장/불러오기
- [x] localStorage(`kmemo_data`) 연동, 기존 데이터 호환 처리 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인에서 문제 발견 → 수정 완료, 재확인 대기)

### CRUD 기능
- [x] 메모 추가/수정/삭제/목록보기, 메인화면 버튼 → 카테고리별 목록 화면 연결 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인에서 문제 발견 → 수정 완료, 재확인 대기)
  - 수정 1: 메모 추가 저장 시 입력칸이 안 비워지던 문제 → 저장 후 같은 화면에서 입력칸 초기화, 바로 다음 메모 작성 가능하도록 변경
  - 수정 2: 뒤로가기 시 목록이 갱신 안 되던 문제 → 뒤로가기 시 목록 다시 렌더링하도록 수정
  - 수정 3: `.view[hidden]`이 `display:flex`에 덮여 화면이 겹쳐 보이던 렌더링 버그 수정

### 카테고리 다중 선택 기능
- [x] 메모 작성/수정 시 공통/개인/사업자/추가 사업체 체크박스 다중 선택 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

---

## 3단계. 캘린더 화면

### 달력 UI
- [x] 메인화면 하단 미니 달력(월 그리드, 이전/다음 달 이동, 메모 작성일에 사용자 지정 색 점 표시) (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)
- [x] 공통/개인/사업자/추가 사업체 클릭 시 카테고리 전용 대시보드(달력+3카드)로 이동 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

### 그날 메모 영역 (왼쪽 위)
- [x] 대시보드 달력에서 날짜 클릭 → "오늘의 메모"/"[N]월[N]일의 메모" 카드에 해당 날짜의 일반 메모 표시, "전체보기" 지원 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

### 월간 할 일 메모 영역 (왼쪽 아래)
- [x] "이번 달 할 일" 카드: 체크박스로 완료 처리, "전체보기" 지원 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

### 사업자 필수 항목 영역 (오른쪽)
- [x] "사업자 필수 항목" 카드: 사용자 선택에 따라 모든 카테고리에 동일하게 표시, "전체보기" 지원 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)
- 참고: 레퍼런스 이미지(`img/메인 메모목록.png`)는 3개 카드를 세로로 쌓은 레이아웃이라 현재도 그 형태로 구현함(원래 명세의 좌/우 2단 배치 대신)

---

## 4단계. 체크리스트 상태 표현

### 완료 체크박스 + 불투명 회색 처리
- [x] "이번 달 할 일"·"사업자 필수 항목" 카드 항목에 체크박스 적용, 완료 시 배경/텍스트 회색 처리 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

### "지켜봐야 함" 표시 (두꺼운 테두리 + 우측 상단 작은 글씨)
- [x] 메모 작성 화면의 "계속 지켜보기" 토글 → 두꺼운 테두리 + "관찰중" 배지 표시 (2026-07-27, 1차 자동화 확인 완료 / 2차 실기기 확인 대기)

---

## 5단계. 알림/주기 기능

### 날짜 직접 지정(복수)

### N일 주기 반복 알림

---

## 6단계. 검색 & PWA

### 메모 내용 기반 검색

### 오프라인 지원 (Service Worker, Manifest)

---

## 7단계. UI 확장
